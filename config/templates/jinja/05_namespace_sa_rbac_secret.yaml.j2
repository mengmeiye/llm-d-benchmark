apiVersion: v1
kind: Namespace
metadata:
  name: {{ namespace.name }}
  labels:
    kubernetes.io/metadata.name: {{ namespace.name }}
    pod-security.kubernetes.io/audit: {{ namespace.labels.podSecurity.audit }}
    pod-security.kubernetes.io/audit-version: latest
    pod-security.kubernetes.io/warn: {{ namespace.labels.podSecurity.warn }}
    pod-security.kubernetes.io/warn-version: latest
spec:
  finalizers:
  - kubernetes
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ serviceAccount.name }}
  namespace: {{ namespace.name }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: inference-perf-job-creator
  namespace: {{ namespace.name }}
rules:
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["create", "get", "list", "watch", "delete", "patch", "update"]
- apiGroups: [""]
  resources: ["serviceaccounts"]
  verbs: ["get"]
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["pods/log"]
  verbs: ["get"]
- apiGroups: [""]
  resources: ["services"]
  verbs: ["get", "list"]
# Allow the metrics collector (collect_metrics.sh) to read the EPP
# metrics-reader Secret so it can present its bearer token when scraping
# the EPP /metrics endpoint (authn/authz filter is on by default).
# Scoped by resourceName to just that Secret (least privilege).
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get"]
  resourceNames: ["{{ router.monitoring.secretName | default('inference-gateway-sa-metrics-reader-secret') }}"]
{% if fma is defined and fma.enabled | default(false) %}
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "patch"]
- apiGroups: ["fma.llm-d.ai"]
  resources: ["inferenceserverconfigs"]
  verbs: ["get"]
{% endif %}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: inference-perf-job-creator-binding
  namespace: {{ namespace.name }}
subjects:
- kind: ServiceAccount
  name: {{ serviceAccount.name }}
  namespace: {{ namespace.name }}
roleRef:
  kind: Role
  name: inference-perf-job-creator
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: inference-perf-restricted-scc
  namespace: {{ namespace.name }}
subjects:
- kind: ServiceAccount
  name: {{ serviceAccount.name }}
  namespace: {{ namespace.name }}
roleRef:
  kind: ClusterRole
  name: system:openshift:scc:restricted
  apiGroup: rbac.authorization.k8s.io
{% if decode is defined and decode.contextLengthRanges is defined and decode.contextLengthRanges | length > 0 %}
---
# Context-length-aware routing: the preprocess script
# (set_llmdbench_environment.py) has each decode pod PATCH its own
# `llm-d.ai/context-length-range` label, which the EPP context-length-aware
# plugin then reads to route by prompt length. That PATCH needs pod-write
# rights in this namespace.
#
# Bound to the modelservice ServiceAccount (`{{ model_id_label }}`), which the
# modelservice chart creates and the decode pods run as -- not the harness
# SA above. Least privilege: only get/patch, only pods, only this namespace.
#
# Required on managed clusters (GKE/EKS/AKS) in particular: their kubeconfigs
# authenticate via an `exec` credential plugin that does not exist in the
# vLLM image, so the preprocess falls back to the pod's projected
# service-account token -- i.e. to this SA's identity.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ model_id_label }}-pod-self-labeler
  namespace: {{ namespace.name }}
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ model_id_label }}-pod-self-labeler
  namespace: {{ namespace.name }}
subjects:
- kind: ServiceAccount
  name: {{ model_id_label }}
  namespace: {{ namespace.name }}
roleRef:
  kind: Role
  name: {{ model_id_label }}-pod-self-labeler
  apiGroup: rbac.authorization.k8s.io
{% endif %}
{% if huggingface.enabled %}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ huggingface.secretName }}
  namespace: {{ namespace.name }}
{% if huggingface.token is defined and huggingface.token %}
stringData:
  {{ huggingface.tokenKey }}: "{{ huggingface.token }}"
{% else %}
data:
  {{ huggingface.tokenKey }}: {{ huggingface.tokenBase64 | b64pad }}
{% endif %}
{% endif %}
