# [REQUIRED]
{% set base_dir = base_dir | default('../') -%}
base_dir: {{ base_dir }}

# [REQUIRED]
values_file:
  path: {{ base_dir }}/config/templates/values/defaults.yaml

# [REQUIRED]
template_dir:
  path: {{ base_dir }}/config/templates/jinja

# [OPTIONAL]
scenario_file:
  path: {{ base_dir }}/config/scenarios/examples/multi-model-optimized-baseline.yaml

# Experiment definitions are shared with the single-model guide - the workload
# shape is the same, only the number of models/pools differs. See:
#   experiments/optimized-baseline.yaml
